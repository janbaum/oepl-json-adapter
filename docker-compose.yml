services:
  epaper-dashboard:
    build: .
    container_name: epaper-dashboard
    restart: unless-stopped
    environment:
      EPAPER_CONFIG: /config/config.yaml
    volumes:
      - ./config:/config:ro
      - ./data:/data
